# 论文seeker · 项目介绍

## 一、项目简介

论文seeker 是一个基于 **OpenAlex 免费学术数据库**的轻量级检索工具。用户只需输入「**大学名称**」和「**专业 / 领域**」两个关键词，系统即可自动识别该高校在对应领域的主要研究人员，并整理出每位研究人员的**研究论文**与**综述文章**，同时提供原文链接，并支持一键导出 **Word 文档**。

## 二、背景与要解决的问题

在学术调研、考研选导师、寻找合作导师、撰写开题综述等场景中，人们常需要快速了解：

> 「某所大学在某个研究方向上有哪些老师？他们发过哪些代表性论文和综述？」

传统做法是在 Google Scholar、Web of Science、知网等平台逐个老师、逐篇文献手工检索，耗时费力且难以汇总。

本系统将这一过程自动化：**一次输入，批量返回**，并附带可点击链接与可导出的 Word 报告。

## 三、核心功能

1. **按「大学 + 领域」检索**：中英文大学名均可（如「清华大学」/「Tsinghua University」）。
2. **自动识别研究人员**：识别该机构在该领域的主要研究者，按发文量与被引量排序。
3. **论文与综述分开列出**：每位研究人员分「论文」「综述」两个标签页展示。
4. **领域代表性综述板块**：单独列出该机构该领域被引最高的综述文章。
5. **链接齐全**：每条文献提供 DOI、PDF、开放获取页 / OpenAlex 页链接。
6. **一键导出 Word**：生成带超链接的 `.docx` 报告（支持导出全部或单个研究人员）。
7. **双语 + 模糊检索**：领域支持中文、英文、简称（AI/ML/NLP…）及拼写容错。

## 四、使用方式

两种使用方式，任选其一：

| 方式 | 操作 | 特点 |
| --- | --- | --- |
| 单文件网页版 | 双击打开 `论文查询.html` | 无需安装，浏览器直连 OpenAlex |
| 服务器版 | 双击 `启动.bat` 或 `npm start` | Node 后端直连 OpenAlex，浏览器访问 `localhost:3000` |

使用步骤：

1. 输入大学名称（中英文均可）；
2. 输入专业 / 领域（中英文、简称均可，支持模糊匹配）；
3. 点击「查询」，等待约 5–20 秒；
4. 查看结果，点击链接跳转原文，或导出 Word 文档。

## 五、技术架构

- **数据源**：OpenAlex（免费、CC0 授权的学术数据库，无需 API Key）。
- **前端**：原生 HTML / CSS / JavaScript（单文件版）；Express 静态服务（服务器版）。
- **后端**：Node.js + Express（服务器版），负责代理 OpenAlex 查询与生成 `.docx`。
- **Word 生成**：`docx` 库（服务器版走本地依赖；单文件版走浏览器 CDN，失败自动降级为 `.doc`）。

## 六、实现原理

1. **大学解析**：调用 OpenAlex `/institutions` 搜索（中英文名均可匹配）。
2. **领域解析**：内置 60+ 中英文领域词典 + 英文简称别名；先词典匹配（精确 → 前缀 → 包含 → 反向包含 → 英文 Levenshtein 模糊），未命中则交给 OpenAlex `/concepts` 相关性检索兜底。
3. **研究人员识别**：抓取「机构 + 领域」被引最高的作品，按作者署名聚合并排序（论文数 + 综述数加权），筛出 Top 12 位研究人员。
4. **论文 / 综述分类**：依据 OpenAlex 文献类型——`article`、`conference-paper` 等归为「论文」，`review` 归为「综述」。
5. **Word 导出**：将结果序列化为带超链接的 Word 文档并下载。

## 七、特点与亮点

- **零门槛**：单文件网页版双击即用，无需安装任何依赖。
- **双语与模糊检索**：中文、英文、简称、拼写错误都能正确识别。
- **综述专门识别**：独立呈现综述文章，避免被高被引论文淹没。
- **链接齐全 + Word 导出**：检索结果可直接引用、直接归档。

## 八、局限与说明

- 数据依赖 OpenAlex，文献以英文为主，中文文献覆盖有限。
- 研究人员识别基于「署名 + 高被引作品聚合」，不区分职称（教授 / 副教授 / 学生）。
- 「综述」指 OpenAlex 中 `type=review` 的文献综述 / 调查类文章。
- 结果仅供参考，正式引用前请核对原始文献。
- 若系统开启了本地代理工具（如 XSUS / Clash 等），单文件版浏览器直连可能被拦截，此时建议使用服务器版（Node 后端直连）或将 OpenAlex 加入代理直连规则。

## 九、项目文件

```
工程/2
├── 论文查询.html      # 单文件网页版（双击即用）
├── 启动.bat            # 服务器版一键启动器
├── server.js           # 服务器版后端
├── public/             # 服务器版前端
├── package.json        # 依赖与脚本
├── 项目介绍.md         # 本文档
└── README.md           # 使用说明
```
